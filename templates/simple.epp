<% | Hash $content | -%>

# Managed by Puppet.

# Any modifications will be gone with the next Puppet synchronization!



<% each(sort(keys($content))) |$k| { -%>
  
<%= $k %> = <%= $content[$k] %>

<% } %>
